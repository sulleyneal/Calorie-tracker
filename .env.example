# Gemini API key — used for two things:
#   1. "Nano banana" (gemini-2.5-flash-image) generates the pretty food photos
#   2. gemini-2.5-flash parses free-text meals ("had a latte and a bagel")
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# USDA FoodData Central API key — accurate calories & macros.
# Get one free at https://fdc.nal.usda.gov/api-key-signup
# DEMO_KEY works for light use.
USDA_API_KEY=DEMO_KEY

# Port for the web server
PORT=3000
